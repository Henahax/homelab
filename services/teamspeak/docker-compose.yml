services:
  teamspeak:
    image: teamspeaksystems/teamspeak6-server:latest
    container_name: teamspeak
    restart: unless-stopped
    environment:
      TSSERVER_LICENSE_ACCEPTED: accept
    ports:
      - "9987:9987/udp" # Voice Port
      - "30033:30033/tcp" # File Transfer
      - "10080:10080/tcp" # Web Query
    volumes:
      - teamspeak-data:/var/tsserver/
#  sinusbot:
#    image: sinusbot/docker
#    container_name: sinusbot
#    restart: unless-stopped
#    environment:
#      - TZ=Europe/Berlin
#    ports:
#      - 8087:8087
#    volumes:
#      - sinusbot-data:/opt/sinusbot/data
#      - sinusbot-scripts:/opt/sinusbot/scripts

volumes:
  teamspeak-data:
    driver: local
    driver_opts:
      type: none
      device: /container-volumes/teamspeak # Host path on your ZFS dataset
      o: bind
#  sinusbot-data:
#    driver: local
#    driver_opts:
#      type: none
#      device: /container-volumes/sinusbot # Host path on your ZFS dataset
#      o: bind
#  sinusbot-scripts:
#    driver: local
#    driver_opts:
#      type: none
#      device: /container-volumes/sinusbot/scripts # Host path on your ZFS dataset
#      o: bind
