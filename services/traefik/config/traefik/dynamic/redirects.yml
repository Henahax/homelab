http:
  routers:
    redirect-gw2-net:
      rule: "Host(`gw2tools.henahax.net`)"
      entryPoints:
        - websecure
      middlewares:
        - gw2.net
        - security-headers@file
      service: noop@internal

    redirect-henahax-de:
      rule: "Host(`henahax.de`) || Host(`www.henahax.de`)"
      entryPoints:
        - websecure
      middlewares:
        - henahax.de
        - security-headers@file
      service: noop@internal

    redirect-gw2-de:
      rule: "Host(`gw2.henahax.de`) || Host(`gw2tools.henahax.de`)"
      entryPoints:
        - websecure
      middlewares:
        - gw2.de
        - security-headers@file
      service: noop@internal

    # Subdomain Redirects
    redirect-contact:
      rule: "Host(`contact.henahax.net`) || Host(`contact.henahax.de`) || Host(`kontakt.henahax.net`) || Host(`kontakt.henahax.de`) || Host(`links.henahax.net`) || Host(`links.henahax.de`)"
      entryPoints:
        - websecure
      middlewares:
        - contact
        - security-headers@file
      service: noop@internal

    redirect-magicfind:
      rule: "Host(`magicfind.henahax.net`) || Host(`magicfind.henahax.de`) || Host(`mf.henahax.net`) || Host(`mf.henahax.de`)"
      entryPoints:
        - websecure
      middlewares:
        - magicfind
        - security-headers@file
      service: noop@internal

    redirect-onlyfans:
      rule: "Host(`onlyfans.henahax.net`) || Host(`onlyfans.henahax.de`) || Host(`of.henahax.net`) || Host(`of.henahax.de`)"
      entryPoints:
        - websecure
      middlewares:
        - onlyfans
        - security-headers@file
      service: noop@internal

    redirect-reset:
      rule: "Host(`reset.henahax.net`) || Host(`reset.henahax.de`)"
      entryPoints:
        - websecure
      middlewares:
        - reset
        - security-headers@file
      service: noop@internal

    redirect-voice:
      rule: "Host(`voice.henahax.net`) || Host(`voice.henahax.de`) || Host(`teamspeak.henahax.net`) || Host(`teamspeak.henahax.de`) || Host(`ts.henahax.net`) || Host(`ts.henahax.de`) || Host(`ts3.henahax.net`) || Host(`ts3.henahax.de`) || Host(`ts6.henahax.net`) || Host(`ts6.henahax.de`)"
      entryPoints:
        - websecure
      middlewares:
        - voice
        - security-headers@file
      service: noop@internal
