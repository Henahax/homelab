services:
  adguardhome:
    container_name: adguardhome
    image: adguard/adguardhome
    restart: unless-stopped

    # 🧯 KEEP DIRECT ACCESS (DNS + UI fallback)
    ports:
      - "53:53/tcp"
      - "53:53/udp"
      - "8053:3000/tcp"

    volumes:
      - adguardhome_config:/opt/adguardhome/conf
      - adguardhome_work:/opt/adguardhome/work

    networks:
      - traefik

    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.adguard-local.rule=Host(`adguard.local`)"
      - "traefik.http.routers.adguard-local.entrypoints=web"
      - "traefik.http.routers.adguard-local.middlewares=lan-only@file"
      - "traefik.http.services.adguard.loadbalancer.server.port=3000"

volumes:
  adguardhome_config:
    driver: local
    driver_opts:
      type: none
      device: /container-volumes/config/adguardhome
      o: bind

  adguardhome_work:
    driver: local
    driver_opts:
      type: none
      device: /container-volumes/work/adguardhome
      o: bind

networks:
  traefik:
    external: true
